const Discord = require('discord.js');
const client = require('../index').client;

module.exports = { // Müll
    queues: {},
    addQueue(guild, deails) {

    },
    clearQueue(guild) {

    },
    play(guild, vc, channel) {
        
    }
}